(() => {
    console.log(`ethereum message signing helpers...`);
    console.log(`run npx ts-node genPrivateKey to get a new random private key`);
    console.log(`run npx ts-node sign to sign a message with a private key`);
    console.log(`run npx ts-node check to verify a signed message with the message and the public key`);
})()